## Instructions

C4 图（Context、Container、Component、Dynamic、Deployment）通过四级抽象对软件架构建模，从系统上下文到组件细节。Mermaid 的 C4 语法与 PlantUML 兼容。

**注意**：C4 是实验性图表类型。语法和属性在未来的版本中可能会发生变化。C4 图使用固定样式——不同皮肤下的 CSS 不会变化，需要使用 `UpdateElementStyle` 和 `UpdateRelStyle` 来覆写样式。

---

### 核心语法速查

#### 元素签名（按图表级别）

**C4Context（系统上下文）**：

| 元素 | 签名 | 参数说明 |
| --- | --- | --- |
| Person | `Person(alias, label, ?descr)` | 别名, "角色名", "描述" |
| Person_Ext | `Person_Ext(alias, label, ?descr)` | 外部人员 |
| System | `System(alias, label, ?descr)` | 别名, "系统名", "描述" |
| SystemDb | `SystemDb(alias, label, ?descr)` | 别名, "数据库名", "描述" |
| SystemQueue | `SystemQueue(alias, label, ?descr)` | 别名, "队列名", "描述" |
| System_Ext | `System_Ext(alias, label, ?descr)` | 外部系统 |
| SystemDb_Ext | `SystemDb_Ext(alias, label, ?descr)` | 外部数据库 |
| SystemQueue_Ext | `SystemQueue_Ext(alias, label, ?descr)` | 外部队列 |
| Boundary | `Boundary(alias, label, ?type)` | 通用边界 |
| Enterprise_Boundary | `Enterprise_Boundary(alias, label)` | 企业边界 |
| System_Boundary | `System_Boundary(alias, label)` | 系统边界 |

**C4Container（容器）**：

| 元素 | 签名 | 参数说明 |
| --- | --- | --- |
| Container | `Container(alias, label, ?techn, ?descr)` | 别名, "容器名", "技术栈", "描述" |
| ContainerDb | `ContainerDb(alias, label, ?techn, ?descr)` | 数据库容器 |
| ContainerQueue | `ContainerQueue(alias, label, ?techn, ?descr)` | 队列容器 |
| Container_Ext | `Container_Ext(alias, label, ?techn, ?descr)` | 外部容器 |
| ContainerDb_Ext | `ContainerDb_Ext(alias, label, ?techn, ?descr)` | 外部数据库容器 |
| ContainerQueue_Ext | `ContainerQueue_Ext(alias, label, ?techn, ?descr)` | 外部队列容器 |
| Container_Boundary | `Container_Boundary(alias, label)` | 容器边界 |

**C4Component（组件）**：

| 元素 | 签名 | 参数说明 |
| --- | --- | --- |
| Component | `Component(alias, label, ?techn, ?descr)` | 别名, "组件名", "技术", "描述" |
| ComponentDb | `ComponentDb(alias, label, ?techn, ?descr)` | 数据组件 |
| ComponentQueue | `ComponentQueue(alias, label, ?techn, ?descr)` | 队列组件 |
| Component_Ext | `Component_Ext(alias, label, ?techn, ?descr)` | 外部组件 |

**C4Deployment（部署）**：

| 元素 | 签名 | 参数说明 |
| --- | --- | --- |
| Deployment_Node | `Deployment_Node(alias, label, ?type, ?descr)` | 别名, "节点名", "OS+规格", "描述" |

#### 关系

```
Rel(from, to, label, ?techn, ?descr)
BiRel(from, to, label, ?techn, ?descr)
Rel_U / Rel_D / Rel_L / Rel_R(from, to, label, ?techn, ?descr)
Rel_Back(from, to, label, ?techn, ?descr)
RelIndex(index, from, to, label, ?techn, ?descr)
```

**重要**：`RelIndex` 的 `index` 参数在 Mermaid 中被忽略——序号由 `RelIndex` 语句的书写顺序决定。C4Dynamic 中每个步骤用一条 `RelIndex` 表示。

#### 样式覆写

```
UpdateElementStyle(elementName, ?bgColor, ?fontColor, ?borderColor, ?shadowing, ?shape, ?sprite, ?techn, ?legendText, ?legendSprite)
UpdateRelStyle(from, to, ?textColor, ?lineColor, ?offsetX, ?offsetY)
UpdateLayoutConfig(?c4ShapeInRow, ?c4BoundaryInRow)
```

**关键**：
- `UpdateElementStyle` 参数顺序：**bgColor → fontColor → borderColor**（先背景、再字体、再边框）
- `UpdateRelStyle` 参数顺序：**textColor → lineColor → offsetX → offsetY**
- 支持命名参数（`$` 前缀）：`$bgColor="#edf5ff"` — 命名参数可以任意顺序，只更新指定的属性
- `UpdateLayoutConfig` 默认值：c4ShapeInRow=4, c4BoundaryInRow=2

#### 不支持的特性

Mermaid C4 目前不支持：Sprites 图标、Tags 标签系统、Links 超链接、Legend 图例、`Lay_U/D/L/R` 布局指令。

---

### 强硬规则：AI 生成 C4 图必须遵守

1. **信息密度**：每个元素必须填写全部有意义的参数，描述要包含"做什么、怎么做、为什么"
2. **关系线必须写协议**：`Rel(a, b, "动作", "协议")`——第 4 参数非空
3. **样式覆写必须**：每个图必须包含 `UpdateElementStyle`（Blueprint 配色）+ `UpdateRelStyle`（关键关系）
4. **元素 > 8 时用 `UpdateLayoutConfig`** 控制布局

### Carbon 配色映射

| 语义角色 | bgColor | fontColor | borderColor |
| --- | --- | --- | --- |
| 内部人员 (Person) | `#f2f4f8` | `#161616` | `#697077` |
| 内部系统 (System) | `#edf5ff` | `#161616` | `#0f62fe` |
| 内部数据库 (SystemDb) | `#d9fbfb` | `#161616` | `#007d79` |
| 内部队列 (SystemQueue) | `#d9fbfb` | `#161616` | `#007d79` |
| 容器 (Container) | `#edf5ff` | `#161616` | `#0f62fe` |
| 数据库容器 (ContainerDb) | `#d9fbfb` | `#161616` | `#007d79` |
| 组件 (Component) | `#edf5ff` | `#161616` | `#0f62fe` |
| 外部系统 (System_Ext) | `#f2f4f8` | `#697077` | `#dde1e6` |
| 外部人员 (Person_Ext) | `#f2f4f8` | `#697077` | `#dde1e6` |
| 部署节点 | `#ffffff` | `#161616` | `#393939` |

参考：`examples/design-system.md`

---

### 示例 1：C4Context — 系统上下文图

```mermaid
C4Context
    title 网上银行系统 — System Context

    Person(customer, "个人客户", "通过 Web 或移动端查看账户、转账、管理理财产品的零售银行客户，日均活跃约 50 万")

    Enterprise_Boundary(bank, "网上银行系统") {
        System(onlinebank, "网上银行", "为个人客户提供账户管理、转账支付、理财产品申购等全部零售银行功能")
        System(backoffice, "后台管理", "供内部运营和客服人员进行客户管理、交易查询、对账和风控操作")
        SystemDb(coredb, "核心账务数据库", "MySQL 8.0 主从+分库分表，存储客户账户、交易流水、产品持仓")
    }

    System_Ext(coresys, "央行支付系统", "中国人民银行 CNAPS2，大额实时支付和小额批量清算的最终通道")
    System_Ext(sms, "短信网关", "阿里云短信服务，发送验证码和交易通知")
    System_Ext(creditsys, "征信系统", "中国人民银行征信中心，查询个人信用报告和评分用于风控审批")

    Rel(customer, onlinebank, "登录、转账、购买理财", "HTTPS/TLS 1.3")
    Rel(onlinebank, coredb, "读写账户和交易数据", "JDBC")
    Rel(onlinebank, coresys, "发起跨行转账和资金清算", "ISO 20022")
    Rel(onlinebank, sms, "触发验证码和动账通知", "HTTPS")
    Rel(onlinebank, creditsys, "查询征信报告和信用评分", "HTTPS/专线")
    Rel(backoffice, coredb, "查询和修正交易记录及客户信息", "JDBC")
    Rel(backoffice, coresys, "对账和异常交易核查", "ISO 20022")

    UpdateElementStyle(customer, $bgColor="#f2f4f8", $fontColor="#161616", $borderColor="#697077")
    UpdateElementStyle(onlinebank, $bgColor="#edf5ff", $fontColor="#161616", $borderColor="#0f62fe")
    UpdateElementStyle(backoffice, $bgColor="#edf5ff", $fontColor="#161616", $borderColor="#0f62fe")
    UpdateElementStyle(coredb, $bgColor="#d9fbfb", $fontColor="#161616", $borderColor="#007d79")
    UpdateElementStyle(coresys, $bgColor="#f2f4f8", $fontColor="#697077", $borderColor="#dde1e6")
    UpdateElementStyle(sms, $bgColor="#f2f4f8", $fontColor="#697077", $borderColor="#dde1e6")
    UpdateElementStyle(creditsys, $bgColor="#f2f4f8", $fontColor="#697077", $borderColor="#dde1e6")

    UpdateRelStyle(onlinebank, coredb, $textColor="#007d79", $lineColor="#007d79")
    UpdateRelStyle(onlinebank, coresys, $textColor="#697077", $lineColor="#dde1e6")

    UpdateLayoutConfig($c4ShapeInRow="4", $c4BoundaryInRow="2")
```

### 示例 2：C4Container — 容器图

```mermaid
C4Container
    title 网上银行系统 — Container Diagram

    Person(customer, "个人客户", "通过浏览器或手机 App 访问银行服务的最终用户")

    System_Boundary(bank, "网上银行系统") {
        Container(webapp, "Web 前端", "Nginx 1.25 + React 18", "服务端渲染 SPA，首屏 <1.5s，支持 i18n")
        Container(gateway, "API Gateway", "Kong 3.6", "统一认证鉴权、限流熔断，峰值 QPS 5 万")
        Container(accountsvc, "账户服务", "Spring Boot 3.2 + Kotlin", "管理客户账户、余额、交易历史，核心领域服务")
        Container(paysvc, "支付服务", "Spring Boot 3.2 + Java 21", "处理内转和跨行转账，支持幂等和 Saga 补偿事务")
        Container(notifsvc, "通知服务", "Node.js 22 + BullMQ", "异步发送短信、推送、邮件，日处理 200 万条")
        ContainerDb(accountdb, "账户数据库", "MySQL 8.0 主从+分库分表", "按客户 ID 哈希分 16 库，存储客户、账户和交易流水")
        ContainerQueue(txqueue, "交易事件队列", "Apache Kafka 3.7", "解耦交易事件，日均消息量 300 万，保留 7 天")
    }

    System_Ext(coresys, "央行支付系统", "中国人民银行 CNAPS2，跨行清算的最终通道")
    System_Ext(sms, "短信网关", "阿里云短信服务，发送验证码和动账通知")

    Rel(customer, webapp, "浏览和操作账户", "HTTPS/TLS 1.3")
    Rel(webapp, gateway, "所有 API 请求经网关转发", "HTTPS + JWT")
    Rel(gateway, accountsvc, "路由到账户服务", "HTTPS + OAuth 2.0")
    Rel(gateway, paysvc, "路由到支付服务", "HTTPS + OAuth 2.0")
    Rel(accountsvc, accountdb, "读写账户和交易数据", "JDBC/HikariCP")
    Rel(paysvc, accountdb, "记录交易和更新余额", "JDBC/HikariCP")
    Rel(paysvc, coresys, "发起跨行转账指令", "ISO 20022/专线")
    Rel(paysvc, txqueue, "发布交易成功事件", "Kafka Producer")
    Rel(notifsvc, txqueue, "消费交易事件触发通知", "Kafka Consumer")
    Rel(notifsvc, sms, "发送短信验证码和动账通知", "HTTPS")

    UpdateElementStyle(customer, $bgColor="#f2f4f8", $fontColor="#161616", $borderColor="#697077")
    UpdateElementStyle(webapp, $bgColor="#edf5ff", $fontColor="#161616", $borderColor="#0f62fe")
    UpdateElementStyle(gateway, $bgColor="#edf5ff", $fontColor="#161616", $borderColor="#0f62fe")
    UpdateElementStyle(accountsvc, $bgColor="#edf5ff", $fontColor="#161616", $borderColor="#0f62fe")
    UpdateElementStyle(paysvc, $bgColor="#edf5ff", $fontColor="#161616", $borderColor="#0f62fe")
    UpdateElementStyle(notifsvc, $bgColor="#edf5ff", $fontColor="#161616", $borderColor="#0f62fe")
    UpdateElementStyle(accountdb, $bgColor="#d9fbfb", $fontColor="#161616", $borderColor="#007d79")
    UpdateElementStyle(txqueue, $bgColor="#d9fbfb", $fontColor="#161616", $borderColor="#007d79")
    UpdateElementStyle(coresys, $bgColor="#f2f4f8", $fontColor="#697077", $borderColor="#dde1e6")
    UpdateElementStyle(sms, $bgColor="#f2f4f8", $fontColor="#697077", $borderColor="#dde1e6")

    UpdateRelStyle(customer, webapp, $textColor="#0f62fe", $lineColor="#0f62fe")
    UpdateRelStyle(paysvc, coresys, $textColor="#697077", $lineColor="#dde1e6")
    UpdateRelStyle(paysvc, txqueue, $textColor="#007d79", $lineColor="#007d79")
    UpdateRelStyle(notifsvc, txqueue, $textColor="#007d79", $lineColor="#007d79")

    UpdateLayoutConfig($c4ShapeInRow="5", $c4BoundaryInRow="1")
```

### 示例 3：C4Component — 组件图

```mermaid
C4Component
    title 账户服务 — Component Diagram

    Container(accountsvc, "账户服务", "Spring Boot 3.2 + Kotlin", "核心领域服务，管理客户账户和交易历史")
    Container_Boundary(svc_boundary, "账户服务内部组件") {
        Component(acct_ctrl, "AccountController", "Spring MVC REST Controller", "接收和校验 HTTP 请求，Bean Validation + @ControllerAdvice 统一异常处理")
        Component(acct_svc, "AccountService", "Spring @Service + DDD", "核心业务逻辑：开户、销户、余额查询，所有操作 @Transactional")
        Component(txn_svc, "TransactionService", "Spring @Service + Saga 编排", "交易流水、幂等校验和补偿事务管理")
        Component(acct_repo, "AccountRepository", "Spring Data JPA + MyBatis", "简单 CRUD 用 JPA，复杂查询用 MyBatis")
        Component(rate_limit, "RateLimiter", "Resilience4j + Redis", "开户和转账接口令牌桶限流，配置热更新")
        Component(event_pub, "DomainEventPublisher", "Kafka + Spring Cloud Stream", "账户变更、交易完成后发布领域事件")
    }
    SystemDb(accountdb, "账户数据库", "MySQL 8.0 主从+分库分表，按客户 ID 哈希分 16 库")
    SystemQueue(txqueue, "交易事件队列", "Apache Kafka 3.7，3 Broker")

    Rel(acct_ctrl, acct_svc, "调用业务逻辑", "同步方法调用")
    Rel(acct_ctrl, rate_limit, "检查限流配额", "Resilience4j API")
    Rel(acct_svc, acct_repo, "持久化账户和交易数据", "Spring Data JPA")
    Rel(acct_svc, event_pub, "发布账户变更事件", "Spring Cloud Stream")
    Rel(acct_svc, txn_svc, "委托交易操作", "同步方法调用")
    Rel(txn_svc, acct_repo, "记录交易流水", "MyBatis")
    Rel(txn_svc, event_pub, "发布交易完成事件", "Spring Cloud Stream")
    Rel(acct_repo, accountdb, "执行 SQL 查询和更新", "JDBC/HikariCP")
    Rel(event_pub, txqueue, "投递领域事件到 Kafka", "Kafka Producer")

    UpdateElementStyle(acct_ctrl, $bgColor="#edf5ff", $fontColor="#161616", $borderColor="#0f62fe")
    UpdateElementStyle(acct_svc, $bgColor="#edf5ff", $fontColor="#161616", $borderColor="#0f62fe")
    UpdateElementStyle(txn_svc, $bgColor="#edf5ff", $fontColor="#161616", $borderColor="#0f62fe")
    UpdateElementStyle(acct_repo, $bgColor="#f6f2ff", $fontColor="#161616", $borderColor="#8a3ffc")
    UpdateElementStyle(rate_limit, $bgColor="#f6f2ff", $fontColor="#161616", $borderColor="#8a3ffc")
    UpdateElementStyle(event_pub, $bgColor="#f6f2ff", $fontColor="#161616", $borderColor="#8a3ffc")
    UpdateElementStyle(accountdb, $bgColor="#d9fbfb", $fontColor="#161616", $borderColor="#007d79")
    UpdateElementStyle(txqueue, $bgColor="#d9fbfb", $fontColor="#161616", $borderColor="#007d79")

    UpdateRelStyle(event_pub, txqueue, $textColor="#007d79", $lineColor="#007d79")
    UpdateRelStyle(acct_svc, event_pub, $textColor="#8a3ffc", $lineColor="#8a3ffc")

    UpdateLayoutConfig($c4ShapeInRow="4", $c4BoundaryInRow="1")
```

### 示例 4：C4Dynamic — 动态图

```mermaid
C4Dynamic
    title 跨行转账 — Dynamic Diagram

    Person(customer, "个人客户", "发起跨行转账的银行客户")
    System(onlinebank, "网上银行", "提供转账功能的 Web 应用")
    Container(accountsvc, "账户服务", "Spring Boot 3.2 + Kotlin", "校验余额和冻结资金")
    Container(paysvc, "支付服务", "Spring Boot 3.2 + Java 21", "创建支付指令并发送央行")
    System_Ext(coresys, "央行支付系统", "中国人民银行 CNAPS2，执行跨行清算")
    System_Ext(sms, "短信网关", "阿里云短信服务，发送交易结果通知")

    RelIndex(1, customer, onlinebank, "1. 填写收款信息并提交转账请求", "HTTPS/TLS 1.3")
    RelIndex(2, onlinebank, accountsvc, "2. 验证付款账户余额是否充足", "HTTPS + JWT")
    RelIndex(3, accountsvc, onlinebank, "3. 返回余额充足，冻结资金", "HTTPS + JWT")
    RelIndex(4, onlinebank, paysvc, "4. 创建支付指令并发起转账", "HTTPS + JWT")
    RelIndex(5, paysvc, coresys, "5. 发送跨行转账报文到央行", "ISO 20022/专线")
    RelIndex(6, coresys, paysvc, "6. 返回清算结果和流水号", "ISO 20022/专线")
    RelIndex(7, paysvc, accountsvc, "7. 根据清算结果扣款或解冻", "HTTPS + JWT")
    RelIndex(8, paysvc, sms, "8. 发送交易结果短信通知", "HTTPS")
    RelIndex(9, sms, customer, "9. 客户收到转账结果短信", "SMS")

    UpdateElementStyle(customer, $bgColor="#f2f4f8", $fontColor="#161616", $borderColor="#697077")
    UpdateElementStyle(onlinebank, $bgColor="#edf5ff", $fontColor="#161616", $borderColor="#0f62fe")
    UpdateElementStyle(accountsvc, $bgColor="#edf5ff", $fontColor="#161616", $borderColor="#0f62fe")
    UpdateElementStyle(paysvc, $bgColor="#edf5ff", $fontColor="#161616", $borderColor="#0f62fe")
    UpdateElementStyle(coresys, $bgColor="#f2f4f8", $fontColor="#697077", $borderColor="#dde1e6")
    UpdateElementStyle(sms, $bgColor="#f2f4f8", $fontColor="#697077", $borderColor="#dde1e6")

    UpdateRelStyle(customer, onlinebank, $textColor="#0f62fe", $lineColor="#0f62fe")
    UpdateRelStyle(paysvc, coresys, $textColor="#697077", $lineColor="#dde1e6")
    UpdateRelStyle(paysvc, sms, $textColor="#697077", $lineColor="#dde1e6")
    UpdateRelStyle(sms, customer, $textColor="#697077", $lineColor="#697077")
```

### 示例 5：C4Deployment — 部署图

```mermaid
C4Deployment
    title 网上银行系统 — Deployment Diagram

    Deployment_Node(dmz, "DMZ 区", "CentOS 9 + Nginx 1.25", "对外暴露入口，仅 443/80 端口，配置 WAF 和 DDoS 防护"){
        Container(webapp, "Web 前端", "Nginx + React 18 SSR", "服务端渲染 SPA，静态资源 CDN 分发")
        Container(gateway, "API Gateway", "Kong 3.6", "统一鉴权、限流、路由、日志采集")
    }
    Deployment_Node(app_cluster, "应用服务集群", "K8s 1.29 + Containerd", "4 台 16C/32G 裸金属节点，运行全部微服务"){
        Container(accountsvc, "账户服务", "Spring Boot 3.2 + Kotlin", "管理客户账户和交易历史")
        Container(paysvc, "支付服务", "Spring Boot 3.2 + Java 21", "处理转账和支付指令")
        Container(notifsvc, "通知服务", "Node.js 22 + BullMQ", "异步发送短信/推送/邮件")
    }
    Deployment_Node(data_cluster, "数据服务集群", "3 节点物理机 + Keepalived VIP", "数据库和消息队列高可用集群"){
        ContainerDb(accountdb, "账户 DB", "MySQL 8.0 1 主 2 从", "存储客户、账户和交易流水数据")
        ContainerQueue(txqueue, "Kafka 集群", "Kafka 3.7 3 Broker", "交易事件异步处理和解耦")
    }

    Rel(webapp, gateway, "API 请求经网关转发", "HTTPS/TLS 1.3")
    Rel(gateway, accountsvc, "路由账户服务请求", "HTTPS + JWT")
    Rel(gateway, paysvc, "路由支付服务请求", "HTTPS + JWT")
    Rel(accountsvc, accountdb, "读写客户账户和交易数据", "JDBC/TCP 内网")
    Rel(paysvc, accountdb, "记录支付交易流水", "JDBC/TCP 内网")
    Rel(paysvc, txqueue, "发布交易完成事件", "Kafka TCP 内网")
    Rel(notifsvc, txqueue, "消费交易事件触发通知", "Kafka TCP 内网")

    UpdateElementStyle(webapp, $bgColor="#edf5ff", $fontColor="#161616", $borderColor="#0f62fe")
    UpdateElementStyle(gateway, $bgColor="#edf5ff", $fontColor="#161616", $borderColor="#0f62fe")
    UpdateElementStyle(accountsvc, $bgColor="#edf5ff", $fontColor="#161616", $borderColor="#0f62fe")
    UpdateElementStyle(paysvc, $bgColor="#edf5ff", $fontColor="#161616", $borderColor="#0f62fe")
    UpdateElementStyle(notifsvc, $bgColor="#edf5ff", $fontColor="#161616", $borderColor="#0f62fe")
    UpdateElementStyle(accountdb, $bgColor="#d9fbfb", $fontColor="#161616", $borderColor="#007d79")
    UpdateElementStyle(txqueue, $bgColor="#d9fbfb", $fontColor="#161616", $borderColor="#007d79")

    UpdateRelStyle(paysvc, txqueue, $textColor="#007d79", $lineColor="#007d79")
    UpdateRelStyle(notifsvc, txqueue, $textColor="#007d79", $lineColor="#007d79")

    UpdateLayoutConfig($c4ShapeInRow="4", $c4BoundaryInRow="2")
```

### 示例 6：带多个企业边界的上下文图

```mermaid
C4Context
    title 保险平台 — System Context

    Person(broker, "保险经纪人", "为客户推荐和配置保险产品，查询多家保险公司的产品库和报价")
    Person(claimant, "理赔申请人", "出险后提交理赔申请、上传资料、查询理赔进度")

    Enterprise_Boundary(insureco, "XX 保险公司") {
        System(core, "核心保单系统", "管理保单全生命周期：承保、批改、续保、退保、理赔")
        SystemDb(policydb, "保单数据库", "Oracle 19c RAC 4 节点，存储全部保单、批单、理赔记录")
        System(underwrite, "核保引擎", "基于 ML 的自动核保决策，标准体秒级出单，非标体转人工")
        System(claim, "理赔系统", "管理理赔流程：报案登记、查勘定损、审核理算、赔付结案")
    }

    Enterprise_Boundary(partner, "合作方") {
        System_Ext(hospital, "医院信息系统", "第三方 HIS，提供就诊记录、诊断证明、费用清单")
        System_Ext(bank, "支付银行", "中国银联/网联，收取保费和支付理赔款的资金通道")
    }

    Rel(broker, core, "查询产品库、获取报价、提交投保", "HTTPS/TLS 1.3")
    Rel(broker, underwrite, "触发风险评估和核保决策", "gRPC/TLS")
    Rel(claimant, claim, "提交理赔申请和上传资料", "HTTPS/TLS 1.3")
    Rel(claimant, core, "查询保单状态和理赔进度", "HTTPS/TLS 1.3")
    Rel(core, policydb, "读写保单、批单、续保数据", "JDBC/Oracle Net")
    Rel(underwrite, policydb, "读取历史赔付数据用于风险建模", "JDBC/Oracle Net")
    Rel(claim, policydb, "记录理赔案件和更新保单状态", "JDBC/Oracle Net")
    Rel(claim, hospital, "获取就诊记录和诊断证明", "HTTPS/HL7 FHIR R4")
    Rel(core, bank, "发起保费扣款和理赔款支付", "HTTPS/银联 ISO 8583")
    Rel(claim, bank, "支付理赔款到客户账户", "HTTPS/银联 ISO 8583")

    UpdateElementStyle(broker, $bgColor="#f2f4f8", $fontColor="#161616", $borderColor="#697077")
    UpdateElementStyle(claimant, $bgColor="#f2f4f8", $fontColor="#161616", $borderColor="#697077")
    UpdateElementStyle(core, $bgColor="#edf5ff", $fontColor="#161616", $borderColor="#0f62fe")
    UpdateElementStyle(underwrite, $bgColor="#edf5ff", $fontColor="#161616", $borderColor="#0f62fe")
    UpdateElementStyle(claim, $bgColor="#edf5ff", $fontColor="#161616", $borderColor="#0f62fe")
    UpdateElementStyle(policydb, $bgColor="#d9fbfb", $fontColor="#161616", $borderColor="#007d79")
    UpdateElementStyle(hospital, $bgColor="#f2f4f8", $fontColor="#697077", $borderColor="#dde1e6")
    UpdateElementStyle(bank, $bgColor="#f2f4f8", $fontColor="#697077", $borderColor="#dde1e6")

    UpdateRelStyle(core, policydb, $textColor="#007d79", $lineColor="#007d79")
    UpdateRelStyle(claim, policydb, $textColor="#007d79", $lineColor="#007d79")
    UpdateRelStyle(claim, hospital, $textColor="#697077", $lineColor="#dde1e6")

    UpdateLayoutConfig($c4ShapeInRow="5", $c4BoundaryInRow="2")
```

---

### AI 生成自检清单

- [ ] C4Context 中 Person 用 3 参数、System 用 3 参数、SystemDb 用 3 参数？
- [ ] C4Container 中 Container 用 4 参数（含 technology）？
- [ ] C4Component 中 Component 用 4 参数（含 technology）？
- [ ] C4Deployment 中 Deployment_Node 用 4 参数（含 type）？
- [ ] 外部系统使用 `System_Ext`、外部人员使用 `Person_Ext`？
- [ ] 每条 Rel 都写了协议（第 4 参数）？
- [ ] `UpdateElementStyle` 参数顺序正确（bgColor → fontColor → borderColor）？
- [ ] `UpdateRelStyle` 参数顺序正确（textColor → lineColor → offsetX → offsetY）？
- [ ] 元素 > 8 时使用了 `UpdateLayoutConfig($c4ShapeInRow=..., $c4BoundaryInRow=...)`？
- [ ] 没有使用 Mermaid 不支持的特性（sprites、tags、links、Lay_U/D/L/R）？
- [ ] `RelIndex` 的 index 值按书写顺序递增？
- [ ] C4Dynamic 中每个步骤用独立的 `RelIndex`？

### 备选方案（Flowchart — 兼容所有 Mermaid 版本）

```mermaid
flowchart TD
    Customer[fa:fa-user Customer]
    WebApp[fa:fa-server Web Application]
    Database[(fa:fa-database Database)]
    Email[fa:fa-envelope Email System]

    Customer -->|Uses| WebApp
    WebApp -->|Reads/Writes| Database
    WebApp -->|Sends| Email

    classDef bpUser fill:#f2f4f8,stroke:#697077,stroke-width:2px
    classDef bpProcess fill:#edf5ff,stroke:#0f62fe,stroke-width:2px
    classDef bpExternal fill:#f2f4f8,stroke:#dde1e6,stroke-width:2px,color:#697077
    classDef bpData fill:#d9fbfb,stroke:#007d79,stroke-width:2px

    class Customer bpUser
    class WebApp bpProcess
    class Email bpExternal
    class Database bpData
```
